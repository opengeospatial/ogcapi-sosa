## SOSA Sampler Feature

A Sampler is "a device that is used by, or implements, a (Sampling) Procedure to create or transform one or more samples." It is a specialisation of [System Properties](../system/) with no additional properties of its own.

This building block wraps [Sampler Properties](../../properties/sampler/) as a GeoJSON feature.
